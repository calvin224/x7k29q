spring:
  application:
    name: weather-service

  datasource:
    url: jdbc:postgresql://localhost:5432/weather
    username: weather
    password: weather

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration